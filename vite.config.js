import { defineConfig } from 'vite'; // eslint-disable-line import/no-extraneous-dependencies

export default defineConfig({
  assetsInclude: ['**/*.woff', '**/*.woff2'],
  base: '/marcy-brand-colors/',
});
